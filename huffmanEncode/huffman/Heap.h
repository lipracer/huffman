#ifndef HEAP_H
#define HEAP_H
typedef int sizeType;

#define lpos(ipos) (2*(ipos)+1)
#define rpos(ipos) (2*(ipos)+2)
#define ppos(ipos) (((ipos)-2)/2)


template <typename T>
void make_heap(T* src, sizeType len)
{

}

#endif